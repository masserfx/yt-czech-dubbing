{
  "name": "voicedub-api",
  "version": "0.1.0",
  "description": "VoiceDub AI — REST API na Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "secrets:setup": "node scripts/setup-secrets.js",
    "test": "node --test src/**/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
